CREATE TABLE landing_raw_app_usage (
    id NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY PRIMARY KEY,
    raw_data CLOB,
    ingestion_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE landing_raw_battery_status (
    id NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY PRIMARY KEY,
    raw_data CLOB,
    ingestion_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
